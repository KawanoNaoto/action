#include <stdio.h>
#include "fight.h"

int ai_action(int enemy_code)
{
    for(int i = 0; i < MOBLENGTH-1; i++){
        if(enemy_code == enemy[i].code){
            c_status this_enemy = enemy[i];
        }
    }
    return 0;
}